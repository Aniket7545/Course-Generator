version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next  # Ensure this matches the output directory of your build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
